name: Deploy Dev Image
run-name: ${{ github.actor }} Deploying Dev 🚀
on:
  push:
    branches:
      - "add-env-files-fix-deploy"
jobs:
  call-reuseable-build-push:
    uses: josh-bradley-digital/elcap-app/.github/workflows/buildPush.yml@add-env-files-fix-deploy
    with:
      env: dev
      k8s-cluster: dev-k8s-sfo3
      deployment: webapp
      approval: true
    secrets: inherit
